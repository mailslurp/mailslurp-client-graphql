# MailSlurp API
# MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
# The version of the OpenAPI document: 6.5.2
# Contact: contact@mailslurp.dev
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

# package mailslurp

# domain_controller_api

type mutation {
  # Add catch all wild card inbox to domain
  # Add a catch all inbox to a domain so that any emails sent to it that cannot be matched will be sent to the catch all inbox generated
  # @param ID! Id_ 
  # @return [DomainDto]
  AddDomainWildcardCatchAll(Id_: ID!): DomainDto

  # Create Domain
  # Link a domain that you own with MailSlurp so you can create email addresses using it. Endpoint returns DNS records used for validation. You must add these verification records to your host provider&#39;s DNS setup to verify the domain.
  # @param CreateDomainOptions createDomainOptions 
  # @return [DomainDto]
  CreateDomain(createDomainOptions: CreateDomainOptions): DomainDto

  # Delete a domain
  # Delete a domain. This will disable any existing inboxes that use this domain.
  # @param ID! Id_ 
  # @return [[String!]]
  DeleteDomain(Id_: ID!): [String!]

  # Update a domain
  # Update values on a domain. Note you cannot change the domain name as it is immutable. Recreate the domain if you need to alter this.
  # @param ID! Id_ 
  # @param UpdateDomainOptions updateDomainOptions 
  # @return [DomainDto]
  UpdateDomain(Id_: ID!, updateDomainOptions: UpdateDomainOptions): DomainDto

}

type query {
  # Get all usable domains
  # List all domains available for use with email address creation
  # @param String! inboxType 
  # @return [DomainGroupsDto]
  GetAvailableDomains(inboxType: String!): DomainGroupsDto

  # Get a domain
  # Returns domain verification status and tokens for a given domain
  # @param ID! Id_ 
  # @param Boolean! checkForErrors 
  # @return [DomainDto]
  GetDomain(Id_: ID!, checkForErrors: Boolean!): DomainDto

  # Get domain issues
  # List domain issues for domains you have created
  # @return [DomainIssuesDto]
  GetDomainIssues(): DomainIssuesDto

  # Get catch all wild card inbox for domain
  # Get the catch all inbox for a domain for missed emails
  # @param ID! Id_ 
  # @return [InboxDto]
  GetDomainWildcardCatchAllInbox(Id_: ID!): InboxDto

  # Get domains
  # List all custom domains you have created
  # @return [[DomainPreview]]
  GetDomains(): [DomainPreview]

  # Get MailSlurp domains
  # List all MailSlurp domains used with non-custom email addresses
  # @param String! inboxType 
  # @return [DomainGroupsDto]
  GetMailSlurpDomains(inboxType: String!): DomainGroupsDto

}

